pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
              echo "clone"
    }
   }
        stage('compile') {
            steps {
                echo "compile"
            }
        }
      stage('test') {
            steps {
              echo "test"
    }
   }
  }
}
